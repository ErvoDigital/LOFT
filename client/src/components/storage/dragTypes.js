// Custom drag MIME type used to identify "an existing asset card is being
// dragged" (as opposed to an OS file drag) across drop targets — asset cards
// (merge/version), folder cards (move-into-folder).
export const ASSET_DRAG_TYPE = "application/x-loft-asset";
